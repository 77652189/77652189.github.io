---
interface Props {
  title: string;
  titleZh: string;
  description: string;
  descriptionZh: string;
  tags: string[];
  stars: number;
  slug: string;
}

const { title, titleZh, description, descriptionZh, tags, stars, slug } = Astro.props;
---

<a href={`/projects/${slug}`} class="block group">
  <div class="bg-slate-900 border border-slate-800 rounded-xl p-6
    hover:border-slate-600 hover:-translate-y-0.5 hover:shadow-xl hover:shadow-black/50
    transition-all duration-200 ease-out">

    <div class="flex items-start justify-between mb-3">
      <div>
        <h3 class="font-mono font-medium text-slate-100 group-hover:text-indigo-400 transition-colors">
          {title}
        </h3>
        <p class="text-xs text-slate-600 mt-0.5">{titleZh}</p>
      </div>
      {stars > 0 && (
        <span class="text-xs text-amber-400 flex items-center gap-1 ml-4 shrink-0 font-mono">
          ★ {stars}
        </span>
      )}
    </div>

    <p class="text-sm text-slate-400 leading-relaxed">{descriptionZh}</p>
    <p class="text-xs text-slate-600 leading-relaxed mt-0.5">{description}</p>

    {tags.length > 0 && (
      <div class="flex flex-wrap gap-1.5 mt-4">
        {tags.map(tag => (
          <span class="text-xs font-mono bg-slate-800 text-slate-500 border border-slate-700/60 px-2 py-0.5 rounded">
            {tag}
          </span>
        ))}
      </div>
    )}
  </div>
</a>
